# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=willcoyne_app
DB_PASSWORD=Dogecoin32!
DB_NAME=willcoyne

# Admin API key for protected endpoints
ADMIN_API_KEY=weinerdawgz

# SMTP for sending emails (e.g., SendGrid SMTP, Mailgun SMTP, or your SMTP server)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass
EMAIL_FROM="William" <will17coyne@gmail.com>

# Optional CORS origin to allow from your frontend (e.g., http://localhost:5173)
FRONTEND_ORIGIN=http://localhost:5173
